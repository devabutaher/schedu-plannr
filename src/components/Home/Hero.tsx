import CalendarComponent from "./Calendar";

const Hero = () => {
  return (
    <div className="flex justify-between">
      <div />
      <CalendarComponent />
    </div>
  );
};

export default Hero;
